import { PhoneFormatePipePipe } from './phone-formate-pipe.pipe';

describe('PhoneFormatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneFormatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
